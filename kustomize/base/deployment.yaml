# deployment.yaml
# Purpose: Base Deployment for the demo-app fake-service workload.
# Security-hardened following OpenShift restricted-v2 SCC best practices.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: demo-app
    app.kubernetes.io/part-of: demo-app
  name: demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
        - name: demo-app
          image: nicholasjackson/fake-service:v0.26.2
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          ports:
            - containerPort: 9090
          env:
            - name: NAME
              value: "demo-app"
            - name: MESSAGE
              value: "Hello from demo-app"
            - name: LISTEN_ADDR
              value: "0.0.0.0:9090"
          readinessProbe:
            exec:
              command: ["wget", "--spider", "-q", "http://localhost:9090/ready"]
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            exec:
              command: ["wget", "--spider", "-q", "http://localhost:9090/health"]
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          securityContext:
            capabilities:
              drop:
                - ALL
            allowPrivilegeEscalation: false
          imagePullPolicy: Always
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      automountServiceAccountToken: false
